import styled from "styled-components";

const ApplicationsStyles = styled.div`
`

export function Applications() {
  return (
    <ApplicationsStyles>
      applications
    </ApplicationsStyles>
  );
}
